Most Minecraft server hosting services do everything for you and charge you for it. I figured out that a general purpose VPS was half the price, the only catch being I had to set it up myself.

So I did. Provisioned a Linux VPS on Hetzner, connected via SSH, set up an SSH key so I never have to touch a password again, configured the firewall, wrote backup scripts, and got a modded Minecraft server running 24/7.

It just runs. Job done.